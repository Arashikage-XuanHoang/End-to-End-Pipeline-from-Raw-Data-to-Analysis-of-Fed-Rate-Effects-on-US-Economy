apache-airflow-providers-postgres==5.10.0
scikit-learn==1.7.2
matplotlib==3.9.0
seaborn==0.13.2



# Tip: Mỗi lần bạn thay đổi requirements.txt → chạy lại:
# docker compose build
# docker compose up -d
# đảm bảo image và container luôn có package mới nhất.

# Để chắc chắn dag-processor có cài apache-airflow-providers-postgres dưới user airflow, bạn làm theo các bước sau:
# Chạy lệnh dưới quyền user airflow:
# docker compose exec --user airflow airflow-dag-processor bash
# pip list | grep apache-airflow-providers-postgres
# Nếu kết quả có dạng : apache-airflow-providers-postgres 6.3.0 -> thành công


